import React from 'react';

//used to conditionally render About Me, Portfolio, Contact, and Resume sections.
function Navbar() {
  return (
    <nav className="Navbar">
      <h1>nav</h1>
    </nav>
  );
}
export default Navbar;